/**
 * Web shell — placeholder.
 *
 * Phase 0 task 5 replaces this with the glass app shell: E1 theme tokens,
 * TanStack Router routes (Chat / Workflows / Context / Settings), the
 * floating dock, and login/signup pages.
 */
export function App() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-semibold tracking-tight">
        invisible-string
      </h1>
    </main>
  );
}
