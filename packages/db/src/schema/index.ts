/**
 * Full database schema: Better Auth tables + product tables.
 * This module is what drizzle-kit reads (see ../../drizzle.config.ts) and
 * what the drizzle client is typed against.
 */
export * from "./auth";
export * from "./product";
