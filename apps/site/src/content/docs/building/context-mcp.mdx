---
title: Context & MCP
section: Building
order: 33
---

import { UnderConstruction } from "../../../components/docs/UnderConstruction";

# Context & MCP

Context is everything the agent can reach at runtime. It comes in two forms:
**MCP connections** — Model Context Protocol servers, at workspace or personal
scope — and authored **skills** with drag-and-drop attachments. Both are
packaged straight into the compiled agent, so a published workflow carries its
tools and knowledge with it.

The Context section includes an MCP registry browser and one-click install. Any
credentials a connection needs are stored write-once and encrypted; the API only
ever reports whether a connection has credentials, never their values.

<UnderConstruction />
